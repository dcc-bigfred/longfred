---
name: longfred etap 8
overview: "Stage 8: domain model + control logic. Domain task consumes INPUT_CHANNEL and WIT_EVENTS, maps InputEvent -> Action -> Cmd (to WIT_COMMANDS), reduces ServerEvent to state. Consist-aware direction (like original), numeric acquisition (digits+#). DoD: cargo build + test proto; on hardware digits+# acquire loco, encoder changes speed."
todos: []
isProject: false
---

# Stage
